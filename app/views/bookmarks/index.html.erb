<a href="javascript:xmlhttp=new XMLHttpRequest();xmlhttp.open('GET', 'http://localhost:3000/bookmarks/add?token=123&title='+ encodeURIComponent(document.title)+'&href=' + encodeURIComponent(document.location.href), true); xmlhttp.send();">Add New Bookmark</a>
<h1>Listing bookmarks</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Url</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bookmarks.each do |bookmark| %>
  <tr>
    <td><%= bookmark.name %></td>
    <td><%= bookmark.url %></td>
    <td><%= link_to 'Show', bookmark %></td>
    <td><%= link_to 'Edit', edit_bookmark_path(bookmark) %></td>
    <td><%= link_to 'Destroy', bookmark, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bookmark', new_bookmark_path %>
